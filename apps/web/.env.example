# Alpacto web (browser-facing). Copy to apps/web/.env.local for local Next.js.

NEXT_PUBLIC_API_URL=http://127.0.0.1:4000
# ZeroDev dashboard project — Google / passkey / OTP need this configured there
NEXT_PUBLIC_ZERODEV_PROJECT_ID=
